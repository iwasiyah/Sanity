// tests/setupTests.ts

import '@testing-library/jest-dom'; // For extended matchers like toBeInTheDocument

// Example of a global setup: mocking global objects, if needed.
// For instance, if you need to mock window or document properties
// globalThis.fetch = jest.fn(() => Promise.resolve({ json: () => Promise.resolve({}) }));

// Add any global setup code here
